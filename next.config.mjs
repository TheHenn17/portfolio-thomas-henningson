/** @type {import('next').NextConfig} */
const nextConfig = {
    basePath: "/portfolio-thomas-henningson",
    output: "export",  // <=== enables static exports
    reactStrictMode: true,
};
  
export default nextConfig;